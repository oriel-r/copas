---
description: Review current changes and commit following git conventions
agent: git_convention_assistant
subtask: true
---
Review the current changes (diff) and commit them following the established conventions.

Classify the type of change, write the commit message following Conventional Commits, and commit.

If the diff mixes multiple unrelated changes, flag it and suggest splitting into separate commits instead of committing everything together.
