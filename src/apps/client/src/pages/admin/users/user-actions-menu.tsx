import React from 'react';

/**
 * Dropdown menu for user actions: Ban, Change Role, Impersonate.
 * Uses adminClient.banUser, adminClient.setRole, adminClient.impersonateUser.
 */
export function UserActionsMenu({ userId }: { userId: string }) {
  throw new Error('Not implemented: User actions dropdown');
}
