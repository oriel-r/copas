import { describe, it } from 'vitest';

describe('RequireAdmin HOC', () => {
  it.todo('should render children if user role is admin');
  it.todo('should redirect to /dashboard if user is not admin');
  it.todo('should redirect to /login if no session exists');
});
