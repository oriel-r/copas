import { describe, it } from 'vitest';

describe('seedSuperAdmin script', () => {
  it.todo('should insert a new admin user if not exists');
  it.todo('should not throw if admin already exists');
});
